# The application uses only Android platform APIs and requires no custom keep rules.
-keepclassmembers class cloud.pcie.openaiq.Live2DAvatarView$AvatarBridge {
    @android.webkit.JavascriptInterface <methods>;
}
